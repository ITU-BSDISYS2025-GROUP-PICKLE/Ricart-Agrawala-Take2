syntax = "proto3";

option go_package = "Ricart-Agrawala-Take2/proto";

message CSRequest {
    int32 node_port = 1;
    int32 lamport = 2;
}

message CSResponse {}

service Node {
    rpc ReceiveCSRequest(CSRequest) returns (CSResponse);
}
